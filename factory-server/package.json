{
  "name": "os26-factory-server",
  "private": true,
  "version": "0.1.0",
  "description": "Mac-mini backend for the OS26 Factory. Accepts /run from factory-ui, spawns the orchestrator (which uses warm tmux Claude REPLs), streams events via SSE.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "cors": "^2.8.5"
  }
}
